% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matching_functions.R
\name{match_people_to_data}
\alias{match_people_to_data}
\title{Match Multiple People to Grouped Data}
\usage{
match_people_to_data(
  to_match,
  data,
  FN_column = "FN",
  LN_column = "LN",
  UPI_column = "UPI",
  group_column = NULL,
  group_value = NULL,
  max_dist = 2,
  method = "osa",
  show_all_fuzzy = FALSE,
  to_match_FN_column = FN_column,
  to_match_LN_column = LN_column,
  to_match_group_column = group_column,
  include_non_matched = FALSE,
  ...
)
}
\arguments{
\item{to_match}{A data frame of people to match, with at least first and last name columns.}

\item{data}{A data frame containing the reference data to match against (must contain names, UPI, and optional group column).}

\item{FN_column}{Column name for first names in `data` (default: `"FN"`).}

\item{LN_column}{Column name for last names in `data` (default: `"LN"`).}

\item{UPI_column}{Column name for Unique Person Identifier in `data` (default: `"UPI"`).}

\item{group_column}{Optional. Column name in `data` representing a grouping (e.g., Department, Team).}

\item{group_value}{Optional. If specified, filters `data` to just this group before matching.}

\item{max_dist}{Maximum allowed string distance for fuzzy matching (default: `2`).}

\item{method}{Method used for computing string distance. Passed to `stringdist::stringdist()` (default: `"osa"`).}

\item{show_all_fuzzy}{Logical; if `TRUE`, shows all fuzzy matches instead of just the best (default: `FALSE`).}

\item{to_match_FN_column}{Column name for first names in the `to_match` data frame (default: same as `FN_column`).}

\item{to_match_LN_column}{Column name for last names in the `to_match` data frame (default: same as `LN_column`).}

\item{to_match_group_column}{Column name in `to_match` that specifies the group (if applicable). Used only when `group_column` is also specified.}

\item{include_non_matched}{Logical; if `TRUE`, includes rows from `to_match` even if no match is found (default: `FALSE`).}

\item{...}{Additional arguments passed to `clean_name()` or internal matching functions.}
}
\value{
A data frame of matched people. Includes input first and last names as `inputFN` and `inputLN`.
        If `include_non_matched = TRUE`, rows with no match will still be returned (likely with NAs).
}
\description{
Matches a set of individuals (first and last names in a data frame) to a target dataset,
optionally within groupings such as teams, departments, or expeditions. This is a batch
version of `match_person_to_grouped_data()`.
}
